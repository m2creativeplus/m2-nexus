#!/bin/sh
echo "🔒 M2 Pre-Commit Guard running..."
# Secret scanning
if command -v gitleaks >/dev/null 2>&1; then
  gitleaks protect --staged --no-banner && echo "✅ No secrets found." || exit 1
else
  echo "⚠️  gitleaks not installed — skipping scan."
fi
echo "✅ Pre-commit checks passed."
